export enum SolanaConfirmation {
  confirmed = 'confirmed',
  finalized = 'finalized',
}

export enum EVMConfirmation {
  confirmed = 'confirmed',
  finalized = 'finalized',
}

export enum SuiConfirmation {
  confirmed = 'confirmed',
  finalized = 'finalized',
}
